This JavaScript file adds event listeners to the heading (h1) and paragraph (p) elements. When the heading is clicked, its color changes to a randomly generated color. When the mouse enters the paragraph, its color changes to a randomly generated color, and when the mouse leaves the paragraph, its color returns to the default. This JavaScript file should be linked to your HTML document using a <script> tag at the bottom of the <body> section or in the <head> section. For example:
